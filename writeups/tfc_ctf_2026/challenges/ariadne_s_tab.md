# Ariadne's Tab

**Flag:** `TFCCTF{0ne_r3d1rect_aw4y_fr0m_th3_tak30v3r}`

# Ariadne's Tab — running log

Append-only. Timestamp every entry.

## Hypotheses
## Findings
## Dead ends
## Limitations
## Next actions

## Brief / category / skills / hypotheses
WEB+ANDROID (mixed) — 491 pts — static — ~2 solves. HARD.
- Brief: 'Mixed Web and Android, running on an API 37 (Google APIs) phone. The flag is in the bot's account. The bot is logged-in on the phone. The solve APK must be submitted on <android submit URL>.'
- Required skills: JDK + Android build tools (GAP) + web exploitation (the APK must drive an exploit against the bot's logged-in session).
- First hypotheses: submit a solve APK that, once auto-installed/run on the bot phone, uses the logged-in session (deeplink/intent/WebView) to leak the flag to a controlled sink.

## Discovery & ideation (2026-09-05)
FILES: ariadnetab.zip -> to_publish/{tenant.yaml, web/(proxy/nginx.conf.template, frontend/, content/, backend/), ariadnetab.apk (68KB)}.
Challenge: mixed Web+Android on an API 37 phone; flag in the bot's account; bot is logged-in on the phone; submit a solve APK.
HYPOTHESES (ranked):
1. Extract + read the whole web app source (backend/frontend/proxy) and tenant.yaml. Find an endpoint the logged-in bot session can reach (XSS/CSRF/IDOR) OR an API that leaks the bot's flag.
2. Reverse ariadnetab.apk (small, 68KB): find deeplinks/intent filters/WebView JS bridges. The solve APK likely must exploit an intent/deeplink (e.g. open a URL in the bot's WebView with an injected token) to steal the flag.
3. Build a solve APK that drives the exploit (Android toolchain ready on host: javac/aapt2/d8/apksigner/android.jar API23).
NEXT: extract web source (already in zip) and read it fully; decompile ariadnetab.apk (jadx/apktool or aapt dump).


## Checkpoint 2026-09-05T17:15Z (post static analysis)
STATIC ANALYSIS COMPLETE (web + APK). Findings:

### Web app (live: ariadnetab.xyz appOrigin, content.ariadnetab.xyz contentDomain)
- Rails 8.1 API + Auth0 (RS256, jwks). config.js: auth0Domain=ariadnetab.eu.auth0.com, clientId=BlPwCjiomX8gHhIXXGACAIMdcdIrFrQk, audience=https://ariadnetab.xyz.
- Endpoints (all under APP_DOMAIN host constraint, Bearer JWT except render):
  - POST /api/documents/list -> own docs [{id,name,mime_type}]
  - POST /api/documents/create {name,mime_type,ciphertext,iv,wrapped_key} (base64) -> own doc
  - POST /api/documents/open {id} -> {cap, wrapped_key} (OWNER ONLY)
  - POST /api/key/get -> own public_key ; POST /api/key/register {public_key} (RSA-3072 SPKI e=65537)
  - POST /api/render {cap} -> {ciphertext, iv, mime_type} (NO owner check; cap-gated only)
  - GET /.well-known/assetlinks.json -> ariadnetab cert 37:43:F6:EA:65:9F:65:1B:91:2C:B1:1B:64:AC:9F:0E:8E:64:04:BE:66:E7:D1:FC:6C:30:2A:CD:62:82:41:EA
- Document crypto: AES-256-GCM key K; wrapped_key=RSA-OAEP(public_key,K); ciphertext=AES-GCM(K,iv). Decrypt needs bot private key + wrapped_key + ciphertext.
- Flag = document in bot's account. Need bot JWT (documents/open+list) AND bot private key (unwrap).
- viewer.html/viewer.js on content domain: parses #cap=&key=, fetch render(cap), decrypt, renders text/html via document.write() (XSS on content domain, but viewer has no bot JWT/keys).
- Capability: Rails message_verifier("document-capabilities"), HMAC secret_key_base (unknown, env). No forge.
- CORS: allow-origin fixed to content domain. No IDOR found in any endpoint.
- No secrets/credentials in zip.

### APK (ariadnetab.apk, com.tfcctf.ariadnetab, jadx-decompiled)
- Device RSA-3072 key pair in SharedPreferences "device": rsa_private_pkcs8 / rsa_public_spki (generated by y.j()).
- MainActivity (exported, launcher) -> Custom Tab https://ariadnetab.xyz/ .
- OpenDocumentActivity (exported): action com.tfcctf.ariadnetab.OPEN_DOCUMENT, extra document_id (strict UUID) -> Custom Tab https://ariadnetab.xyz/#/open/<uuid>.
- Chrome Custom Tabs postMessage channel: page sees event.origin "android-app://ariadnetab.xyz" + event.ports[0] (MessagePort). JS bridge: only method "key.get" -> returns {privateKey, publicKey} to the page. Other methods -> method_not_found. s.java = RPC handler; t.java = ICustomTabsCallback binder.
- key pair also saved by web app.js to Chrome localStorage (rsaPrivateKeyPkcs8/rsaPublicKeySpki) on app origin.
- assetlinks.json: handle_all_urls + use_as_origin for com.tfcctf.ariadnetab.

### Platform (android.koth.pro)
- Team invite token (from CTFd /team): b899e65f20f9480b01bdf93cc1a9f058189dc780ad329da645610d3cdf1d7063
- /provision POST (challenge name + APK file) -> session_id; logs via WebSocket /ws/<session_id> (logcat, tag TFCCTF). /session/<id>/logs after finish. Rate limit 429 -> 30s.
- Bot phone: API 37 (Google APIs). Bot logged-in on Chrome; ariadnetab app installed.

### Leading hypothesis
Solve APK runs on bot phone, must obtain flag into its own process and Log.d("TFCCTF", flag). Needs bot JWT + bot private key (both live in bot's Chrome session/localStorage + ariadnetab SharedPreferences, sandboxed). Likely chain: drive bot Chrome via Custom Tabs/deeplink + postMessage(key.get) + viewer XSS to exfiltrate. Next: probe APK to learn emulator env (root? packages? sandbox?), then exploit.

### Toolchain (verified pre-crash)
javac (17) + d8 (java -cp /opt/r8.jar com.android.tools.r8.D8) + aapt2 + zipalign + apksigner + keytool. android.jar API23 only at /usr/lib/android-sdk/platforms/android-23/android.jar. jadx installed at /root/tools/jadx/bin/jadx.


## Checkpoint 2026-09-06T04:45Z (breakthrough from ShipMe sibling)
KEY FACTS LEARNED from sibling ctf-shipme (solved ShipMe with same platform):
- The android.koth.pro harness GRANTS WRITE_SECURE_SETTINGS to the submitted APK (SELF_ENABLE_OK). => our app can self-enable its own AccessibilityService via Settings.Secure.putString(ENABLED_ACCESSIBILITY_SERVICES + ACCESSIBILITY_ENABLED).
- AccessibilityService route WORKS: service reads active window trees (getWindows() + getRootInActiveWindow()), extracts text, FLAG_SECURE does NOT block the a11y tree.
- Exfil: Log.i("TFCCTF", flag) -> platform logcat captures tag TFCCTF.
- Solved flag example: TFCCTF{parcelables_are_safer_not_safe}.
- The submitted APK must be a DIFFERENT package than the pre-installed challenge app (me.ship/ariadnetab conflict => UPDATE_INCOMPATIBLE). So do NOT use package com.tfcctf.ariadnetab.
- Build must targetSdk >= 24 (API37 min installable targetSdk). ShipMe solver used targetSdk 28; our first probe used targetSdk 23 and got "Run failed" at install. Use targetSdk 28.
- D8 must receive ALL .class files (incl anonymous inner classes) or NoClassDefFoundError at runtime.
- ~20 min rate limit between submissions.

SOLVE PLAN (Ariadne's Tab):
1. Solver APK (package com.solver.ariadne, targetSdk 28) self-enables a11y service.
2. MainActivity drives the bot's Chrome: VIEW intent https://ariadnetab.xyz/ -> App Links -> ariadnetab app -> bot's logged-in Chrome Custom Tab -> app.js auto-loads the bot's document list.
3. a11y service polls windows, finds "Open" buttons, performs ACTION_CLICK -> bot session opens+decrypts the flag document -> viewer renders TFCCTF{...}.
4. a11y service detects TFCCTF{...} in the tree, Log.i("TFCCTF","FLAG=...").
Built: solve/ariadne_solver.apk (21KB, com.solver.ariadne). Submit via /provision (bg retry). First probe (com.example.probe, targetSdk23) failed install - retargeted to 28.


## Checkpoint 2026-09-06T05:34:22.522249
- Solver v1 (a11y) RAN on bot phone (session fbythbq95r): SELF_ENABLE_FAIL=SecurityException WRITE_SECURE_SETTINGS => platform does NOT grant it for Ariadne's Tab (ShipMe did). A11y route DEAD.
- CCT_LAUNCH_OK proves ariadnetab app (com.tfcctf.ariadnetab) IS pre-installed.
- Confirmed via Chromium source: requestPostMessageChannel -> verifyOriginForSession (null for non-Chrome) -> verifyAndInitializeWithPostMessageOriginForSession(RELATION_USE_AS_ORIGIN) -> DAL check of package_name + sha256_cert_fingerprint. Our APK cannot match cert 3743f6ea... (custom release cert). So postMessage-as-native is INFEASIBLE.
- Also the web page only sends key.get requests (never JWT/privateKey to app).
- Next: rootprobe.apk (com.solver.rootprobe, targetSdk28) checks su/root, reads ariadnetab SharedPreferences device.xml + Chrome localStorage JWTs via su; if it gets privkey+JWT, does full API+decrypt chain and logs PLAIN/FLAG. Submitted (bg retry), expect ~05:42.


## FINAL - PARKED 2026-09-06T06:22:03.311706 (no solve)
Three exploit routes confirmed DEAD on the bot phone (empirically):

1. ACCESSIBILITY / screen-read: WRITE_SECURE_SETTINGS is NOT granted to the submitted APK for
   Ariadne's Tab (solver fbythbq95r: SELF_ENABLE_FAIL=SecurityException: Permission denial, must
   have one of: [android.permission.WRITE_SECURE_SETTINGS]). Ship Me granted it; Ariadne does not.

2. ROOT / su: no usable root. rootprobe (8ubp1f1vl5): SU_ID='exec su: Permission denied',
   DIRECT_DEVICE exists=false canRead=false, DEVICE_XML/CHROME_JWTS unreadable via su.
   Emulator: ro.build.type=userdebug, SDK 37; ariadnetab app installed (versionCode 6) and
   debuggable=false. So ariadnetab SharedPreferences (bot RSA private key) and Chrome localStorage
   (JWT + rsa keys) are sandboxed and unreachable.

3. POST-MESSAGE as native: requires the exact Digital-Asset-Links use_as_origin binding.
   Verified in Chromium source: requestPostMessageChannel -> verifyOriginForSession (null for
   non-Chrome callers) -> verifyAndInitializeWithPostMessageOriginForSession(RELATION_USE_AS_ORIGIN)
   -> DAL check of package_name + sha256_cert_fingerprint against
   https://ariadnetab.xyz/.well-known/assetlinks.json. That file pins package com.tfcctf.ariadnetab
   with cert SHA-256 3743f6ea659f651b912cb11b64ac9f0e8e6404be66e7d1fc6c302acd628241ea (a custom
   release cert, CN='Ariadne's Tab'; NOT the debug keystore). We cannot reproduce that cert, and
   the ariadnetab app is pre-installed so a same-package APK would fail UPDATE_INCOMPATIBLE.
   Therefore we cannot produce event.origin 'android-app://ariadnetab.xyz'.

Additional bridge detail: the web page's only native RPC is 'key.get' (native-rpc.js). The page
REQUESTS the device private key FROM the app; it NEVER sends the JWT or the private key to the
app. So even a hypothetical matching app could not ask the page for the JWT/privateKey; it could
only respond to key.get with keys it already possesses.

Recap of the target chain (unused): flag = encrypted document in bot's account. Need bot JWT
(documents/open + list, in Chrome localStorage) + bot RSA-3072 private key (ariadnetab
SharedPreferences / Chrome localStorage) to unwrap the AES key and decrypt (viewer path).
Viewer has a text/html document.write XSS on content.ariadnetab.xyz but that origin has no bot
session and no native channel. OPEN_DOCUMENT deeplink opens #/open/<uuid> in the bot's Chrome but
document ids are server-generated UUIDs (unobservable without a11y).

Attempts: probe(com.example.probe, tSdk23) install-failed; solver a11y (fbythbq95r) ran but no
WRITE_SECURE_SETTINGS; rootprobe (8ubp1f1vl5) ran but no su/root. Platform 'Preparing challenge'
is flaky (~1/3 success) - retry on that step when resuming.
